// This file is autogenerated. Do not modify directly.

package scenarios

import (
    "reflect"
{% for version, _ in apis.items() %}
    {{ version }} "{{ module }}/api/{{ version }}/datadog"
{%- endfor %}
)

var apiMappings = map[string]map[string]reflect.Value{
	{%- for version, allApis in apis.items() %}
	"{{ version }}": {
	    {%- for name in allApis.keys() %}
	    "{{ name.replace(" ", "") }}Api": reflect.ValueOf({{ version }}.New{{ name.replace(" ", "") }}Api),
	    {%- endfor %}
	},
	{%- endfor %}
}